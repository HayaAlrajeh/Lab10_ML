# SVM Classification Lab

This lab applies the Support Vector Machine (SVM) algorithm using the Iris dataset. The notebook includes data visualization, model training, prediction, and performance evaluation. GridSearchCV was also used to improve the model accuracy.
